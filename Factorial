#include <iostream>
using namespace std;
int main()
{
	int n, fact=1, i;

	do {
	cout << "Ingresar numero de elmentos: ";
	cin >> n;
	} while (n < 0);

	for (i = 2; i <= n; i++) 
	{
		fact = fact * i;
	}
	cout << "La multiplicacion es: " << fact;

	return 0;
}
